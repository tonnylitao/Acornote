<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="android.view.View" />
        <variable name="viewModel" type="tonnysunm.com.acronote.activity.folder.ItemViewModel" />
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/item_title_textview"
            android:layout_height="wrap_content"
            android:layout_width="0dp"
            android:textSize="28sp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="5dp"
            android:layout_marginLeft="5dp"
            android:layout_marginBottom="5dp"
            android:layout_marginRight="@{viewModel.hasImage() ? @dimen/item_right_with_image : @dimen/item_right_without_image}"
            android:text="@{viewModel.title}"
            tools:text="Hello"
            />

        <TextView
            android:id="@+id/item_des_textview"
            android:layout_height="wrap_content"
            android:layout_width="0dp"
            android:text="@{viewModel.title}"
            android:textSize="20sp"
            app:layout_constraintTop_toBottomOf="@+id/item_title_textview"
            app:layout_constraintLeft_toRightOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_alignParentEnd="true"
            android:layout_marginLeft="5dp"
            android:layout_marginBottom="5dp"
            android:layout_marginRight="@{viewModel.hasImage() ? @dimen/item_right_with_image : @dimen/item_right_without_image}"
            />

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/ic_link_tips"
            android:visibility="@{viewModel.hasUrl() ? View.VISIBLE : View.GONE}"
            />

        <ImageView
            android:layout_width="40dp"
            android:layout_height="40dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginRight="5dp"
            android:layout_marginTop="5dp"
            android:background="@color/blue"
            android:visibility="@{viewModel.hasImage() ? View.VISIBLE : View.GONE}"
            />

        <View style="@style/Divider"/>

    </android.support.constraint.ConstraintLayout>
</layout>