<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context="tonnysunm.com.acornote.ui.folder.FolderActivity">

    <item
        android:id="@+id/menu_flip"
        android:icon="@drawable/ic_flip"
        app:showAsAction="always"
        />
    <item
        android:id="@+id/menu_play"
        android:icon="@drawable/ic_paly"
        app:showAsAction="always"
        />
    <item
        android:id="@+id/menu_url"
        android:icon="@drawable/ic_url"
        app:showAsAction="always"
        />
    <item
        android:id="@+id/menu_move"
        android:title="Move"
        app:showAsAction="collapseActionView"
        />
</menu>